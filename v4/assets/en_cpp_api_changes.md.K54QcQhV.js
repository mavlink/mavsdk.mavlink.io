import{_ as e,o as a,c as i,ag as n}from"./chunks/framework.D1p_5hVZ.js";const k=JSON.parse('{"title":"API Changes","description":"","frontmatter":{"github_branch":"v4"},"headers":[],"relativePath":"en/cpp/api_changes.md","filePath":"en/cpp/api_changes.md"}'),t={name:"en/cpp/api_changes.md"};function o(l,s,d,p,r,c){return a(),i("div",null,s[0]||(s[0]=[n(`<h1 id="api-changes" tabindex="-1">API Changes <a class="header-anchor" href="#api-changes" aria-label="Permalink to &quot;API Changes&quot;">​</a></h1><p>This page tracks changes between versions.</p><p>It covers both breaking (incompatible) and non-breaking changes.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>All examples below are assuming <code>using namespace mavsdk</code>.</p></div><h2 id="semantic-versioning" tabindex="-1">Semantic Versioning <a class="header-anchor" href="#semantic-versioning" aria-label="Permalink to &quot;Semantic Versioning&quot;">​</a></h2><p>MAVSDK follows <a href="https://semver.org/" target="_blank" rel="noreferrer">semver/Semantic Versioning</a> conventions where as possible.</p><p>The version number has the format: <strong>major.minor.patch</strong>. Individual parts of the number are iterated when the:</p><ul><li><strong>major</strong>: API is changed, or functionality is removed.</li><li><strong>minor</strong>: API is extended, functionality is added.</li><li><strong>patch</strong>: API is not changed, functionality is not changed, but a bug is fixed.</li></ul><p>This means that breaking changes to the API result in a bump of the major version number (e.g. 1.4.3 to 2.0.0).</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Bumping of the major version is unrelated to the stability of the library. E.g. v2.0.0 is not by definition more stable than v1.4.18. It just means that the API has changed with v2. As development is carried on, stability is likely increasing whenever the minor or patch versions increase as incremental fixes are added.</p></div><h2 id="v4" tabindex="-1">v4 <a class="header-anchor" href="#v4" aria-label="Permalink to &quot;v4&quot;">​</a></h2><p>Most application code builds against v4 after renaming the includes and adapting to a few type changes. This section lists what changed, starting with what breaks the build.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Using MAVSDK from Python? From v4, the <code>mavsdk</code> package on PyPI is a new native binding. See <a href="./../python/migration.html">Migrating from MAVSDK-Python</a>.</p></div><h3 id="headers-are-now-hpp" tabindex="-1">Headers are now .hpp <a class="header-anchor" href="#headers-are-now-hpp" aria-label="Permalink to &quot;Headers are now .hpp&quot;">​</a></h3><p>Since v4 added a C API, we needed to make sure the C++ headers were clearly distinct. Therefore, all public C++ headers were renamed from <code>.h</code> to <code>.hpp</code>, both in core and in the plugins:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;mavsdk/mavsdk.h&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;mavsdk/plugins/action/action.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v4</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;mavsdk/mavsdk.hpp&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;mavsdk/plugins/action/action.hpp&gt;</span></span></code></pre></div><p>There are no compatibility headers. The only exception is <code>mavsdk/mavsdk_export.h</code>, which keeps its name because it is shared with the C bindings.</p><p>The install directories are unchanged.</p><h3 id="building-and-linking" tabindex="-1">Building and linking <a class="header-anchor" href="#building-and-linking" aria-label="Permalink to &quot;Building and linking&quot;">​</a></h3><ul><li><strong>C++ standard:</strong> the library itself is now built as C++20. Applications need C++17 or later to use the headers, as with v3.</li><li><strong>Building from source:</strong> the C++ project moved into the <code>cpp/</code> directory. Configure with <code>cmake -S cpp -B build</code>, use <code>add_subdirectory(MAVSDK/cpp)</code> instead of <code>add_subdirectory(MAVSDK)</code>, and set <code>SOURCE_SUBDIR cpp</code> with <code>FetchContent</code>. Scripts moved from <code>tools/</code> to <code>cpp/tools/</code>.</li></ul><h3 id="dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#dependencies" aria-label="Permalink to &quot;Dependencies&quot;">​</a></h3><ul><li>Without the superbuild, the system dependencies are now <code>nlohmann-json3-dev</code>, <code>libfmt-dev</code> and <code>libasio-dev</code> instead of <code>libjsoncpp-dev</code>.</li></ul><h3 id="packages" tabindex="-1">Packages <a class="header-anchor" href="#packages" aria-label="Permalink to &quot;Packages&quot;">​</a></h3><ul><li><strong>Prebuilt packages:</strong> <code>.deb</code> packages are no longer provided for Ubuntu 20.04 and Debian 11.</li></ul><h3 id="system-ids-are-32-bit" tabindex="-1">System IDs are 32 bit <a class="header-anchor" href="#system-ids-are-32-bit" aria-label="Permalink to &quot;System IDs are 32 bit&quot;">​</a></h3><p>In preparation for 32-bit MAVLink system IDs, system IDs in the API are now <code>uint32_t</code> instead of <code>uint8_t</code>:</p><table tabindex="0"><thead><tr><th>API</th><th>v3</th><th>v4</th></tr></thead><tbody><tr><td><code>Mavsdk::Configuration(system_id, component_id, always_send_heartbeats)</code></td><td><code>uint8_t system_id</code></td><td><code>uint32_t system_id</code></td></tr><tr><td><code>Mavsdk::Configuration::get_system_id()</code> / <code>set_system_id()</code></td><td><code>uint8_t</code></td><td><code>uint32_t</code></td></tr><tr><td><code>System::get_system_id()</code></td><td><code>uint8_t</code></td><td><code>uint32_t</code></td></tr><tr><td><code>System::init(system_id, component_id)</code></td><td><code>uint8_t system_id</code></td><td><code>uint32_t system_id</code></td></tr><tr><td><code>MavlinkAddress::system_id</code></td><td><code>uint8_t</code></td><td><code>uint32_t</code></td></tr><tr><td><code>MavlinkPassthrough::CommandLong::target_sysid</code>, <code>CommandInt::target_sysid</code></td><td><code>uint8_t</code></td><td><code>uint32_t</code></td></tr><tr><td><code>MavlinkPassthrough::get_our_sysid()</code>, <code>get_target_sysid()</code>, <code>make_command_ack_message()</code></td><td><code>uint8_t</code></td><td><code>uint32_t</code></td></tr></tbody></table><p>Component IDs stay <code>uint8_t</code>.</p><p>MAVSDK does not support system IDs above 255 yet. Configuring one, or passing one to <code>System::init()</code>, logs an error and aborts. <code>MavlinkDirect::send_message()</code> returns <code>Result::InvalidField</code> for target IDs above 255, where v3 silently truncated them.</p><h3 id="plugin-api-changes" tabindex="-1">Plugin API changes <a class="header-anchor" href="#plugin-api-changes" aria-label="Permalink to &quot;Plugin API changes&quot;">​</a></h3><ul><li><p><strong>Telemetry:</strong> <code>home()</code> and <code>subscribe_home()</code> now provide a <code>Telemetry::HomePosition</code> instead of a <code>Telemetry::Position</code>. It has the same <code>latitude_deg</code>, <code>longitude_deg</code>, <code>absolute_altitude_m</code> and <code>relative_altitude_m</code> fields, plus the local position, orientation and approach vector from <code>HOME_POSITION</code>. Code using <code>auto</code> and those fields keeps compiling.</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v3</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Telemetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Position home </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> telemetry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">home</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v4</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Telemetry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::HomePosition home </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> telemetry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">home</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div></li><li><p><strong>Shell:</strong> <code>send()</code> takes the device to send to, and received data says which device it came from. <code>Shell::Device::Shell</code> is the MAVLink shell used in v3:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shell.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribe_receive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([](</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">string</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> output</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::cout </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> output; });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shell.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ver all&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v4</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shell.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">subscribe_receive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([](</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Shell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Receive</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> receive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::cout </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> receive.data; });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">shell.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ver all&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Shell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Device</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Shell);</span></span></code></pre></div></li><li><p><strong>Info:</strong> <code>get_flight_information()</code> was removed. Use <code>subscribe_flight_information()</code> instead.</p></li><li><p><strong>Ftp:</strong> <code>ListDirectoryData</code> has a single <code>entries</code> list of <code>Ftp::FilesystemEntry</code>, with the name, type (file or directory), size and modification time of each entry, instead of the separate <code>dirs</code> and <code>files</code> lists of names.</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v3</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> auto&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> file : data.files) { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::cout </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v4</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> auto&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> entry : data.entries) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (entry.entry_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Ftp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">FilesystemEntry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">EntryType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::File) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::cout </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> entry.name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot; (&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> entry.size_bytes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot; bytes)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li></ul><h3 id="enum-values-follow-the-proto-definitions" tabindex="-1">Enum values follow the proto definitions <a class="header-anchor" href="#enum-values-follow-the-proto-definitions" aria-label="Permalink to &quot;Enum values follow the proto definitions&quot;">​</a></h3><p>Generated enums now carry the numeric values from the proto definitions. Before, they were numbered sequentially. Names are unchanged, so this only matters to code that stores, sends or casts enum values as integers. These enums changed:</p><table tabindex="0"><thead><tr><th>Enum</th><th>Changed values (v3 → v4)</th></tr></thead><tbody><tr><td><code>Camera::Storage::StorageType</code>, <code>CameraServer::StorageInformation::StorageType</code></td><td><code>Hd</code> 4 → 7, <code>Other</code> 5 → 254</td></tr><tr><td><code>Failure::FailureUnit</code></td><td><code>SystemBattery</code> 9 → 100, <code>SystemMotor</code> 10 → 101, <code>SystemServo</code> 11 → 102, <code>SystemAvoidance</code> 12 → 103, <code>SystemRcSignal</code> 13 → 104, <code>SystemMavlinkSignal</code> 14 → 105</td></tr><tr><td><code>Mission::Result</code></td><td><code>UnsupportedMissionCmd</code> 9 → 11, <code>TransferCancelled</code> 10 → 12, <code>NoSystem</code> 11 → 13, <code>Next</code> 12 → 14, <code>Denied</code> 13 → 15, <code>ProtocolError</code> 14 → 16, <code>IntMessagesNotSupported</code> 15 → 17</td></tr><tr><td><code>MissionRawServer::Result</code></td><td><code>UnsupportedMissionCmd</code> 9 → 11, <code>TransferCancelled</code> 10 → 12, <code>NoSystem</code> 11 → 13, <code>Next</code> 12 → 14</td></tr><tr><td><code>Rtk::Result</code></td><td><code>NoSystem</code> 3 → 5, <code>ConnectionError</code> 4 → 6</td></tr><tr><td><code>Telemetry::Odometry::MavFrame</code>, <code>TelemetryServer::Odometry::MavFrame</code></td><td><code>BodyNed</code> 1 → 8, <code>VisionNed</code> 2 → 16, <code>EstimNed</code> 3 → 18</td></tr></tbody></table><p>The new <code>Shell::Device</code> enum is not sequential either, so don&#39;t derive its values from their position.</p><h3 id="mavlink-c-headers-are-opt-in" tabindex="-1">MAVLink C headers are opt-in <a class="header-anchor" href="#mavlink-c-headers-are-opt-in" aria-label="Permalink to &quot;MAVLink C headers are opt-in&quot;">​</a></h3><p>The MAVSDK headers no longer include the MAVLink C headers, so <code>mavlink_message_t</code>, <code>MAV_TYPE</code>, <code>MAV_COMP_ID_...</code> and the other MAVLink C types, functions and macros are not available anymore just by including <code>mavsdk.hpp</code>.</p><p>The APIs that are built on those types, <code>MavlinkPassthrough</code> and <code>Mavsdk::intercept_incoming_messages_async()</code> / <code>intercept_outgoing_messages_async()</code>, are deprecated, and only available if <code>MAVSDK_ENABLE_MAVLINK_C_API</code> is defined before including MAVSDK. Including <code>mavlink_passthrough.hpp</code> without it fails with an error.</p><p>To keep using them for now, define it for your target:</p><div class="language-cmake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cmake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">target_compile_definitions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(your_app </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PRIVATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MAVSDK_ENABLE_MAVLINK_C_API)</span></span></code></pre></div><p>This includes the MAVLink C headers like before.</p><p>Alternatively, add the define before the include:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MAVSDK_ENABLE_MAVLINK_C_API</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;mavsdk/mavsdk.hpp&gt;</span></span></code></pre></div><p>To move away from these APIs, use <a href="./guide/mavlink_direct.html">MavlinkDirect</a> instead of MavlinkPassthrough, and <code>Mavsdk::subscribe_incoming_messages_json()</code> / <code>subscribe_outgoing_messages_json()</code> instead of the interception. Unlike MavlinkPassthrough, MavlinkDirect does not depend on the MAVLink dialect MAVSDK was built with.</p><p>For how it is used in practice, see the examples:</p><table tabindex="0"><thead><tr><th>Example</th><th>Shows</th></tr></thead><tbody><tr><td><a href="https://github.com/mavlink/MAVSDK/tree/main/cpp/examples/mavlink_direct" target="_blank" rel="noreferrer">mavlink_direct</a></td><td>Subscribing to a message (GPS_RAW_INT), and stats about all arriving messages</td></tr><tr><td><a href="https://github.com/mavlink/MAVSDK/tree/main/cpp/examples/mavlink_direct_sender" target="_blank" rel="noreferrer">mavlink_direct_sender</a></td><td>Sending a message (OBSTACLE_DISTANCE)</td></tr><tr><td><a href="https://github.com/mavlink/MAVSDK/tree/main/cpp/examples/mavlink_direct_sender_custom" target="_blank" rel="noreferrer">mavlink_direct_sender_custom</a></td><td>Sending a message that MAVSDK does not know yet, by loading its XML definition</td></tr><tr><td><a href="https://github.com/mavlink/MAVSDK/tree/main/cpp/examples/sniffer" target="_blank" rel="noreferrer">sniffer</a></td><td>Intercepting all traffic with <code>subscribe_incoming_messages_json()</code></td></tr></tbody></table><h3 id="mav-type-is-an-enum" tabindex="-1">MAV_TYPE is an enum <a class="header-anchor" href="#mav-type-is-an-enum" aria-label="Permalink to &quot;MAV_TYPE is an enum&quot;">​</a></h3><p><code>Mavsdk::Configuration::get_mav_type()</code> and <code>set_mav_type()</code> use the new <code>MavType</code> enum instead of a <code>uint8_t</code>, and <code>to_vehicle_from_mav_type()</code> takes a <code>MavType</code> instead of a <code>MAV_TYPE</code>. The values of <code>MavType</code> are the same as for <code>MAV_TYPE</code>.</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_mav_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MAV_TYPE_FIXED_WING);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// v4</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">configuration.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_mav_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MavType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::FixedWing);</span></span></code></pre></div><h3 id="behaviour-changes" tabindex="-1">Behaviour changes <a class="header-anchor" href="#behaviour-changes" aria-label="Permalink to &quot;Behaviour changes&quot;">​</a></h3><p><strong>Nothing handed out by Mavsdk may outlive it.</strong> Destroying <code>Mavsdk</code> while a <code>System</code>, <code>ServerComponent</code> or plugin is still alive now aborts with an explanation. In v3, this was undefined behaviour that crashed later or went unnoticed. Look out for member declaration order, since members are destroyed in reverse:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Aborts on destruction: _action is destroyed after _mavsdk.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Drone</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Action</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _action;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Mavsdk _mavsdk{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mavsdk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Configuration{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ComponentType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::GroundStation}};</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Fine: _mavsdk is declared first, so it is destroyed last.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Drone</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Mavsdk _mavsdk{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mavsdk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::Configuration{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ComponentType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::GroundStation}};</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::optional</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Action</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _action;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>The same applies to systems or plugins kept in globals, statics or containers that live longer than the <code>Mavsdk</code> instance.</p><p><strong>Slow subscribers lose updates, not results.</strong> User callbacks still run one at a time on a single thread. In v3, once 100 callbacks were queued, any callback was dropped, including the result of a command, which could make a blocking call such as <code>Action::arm()</code> hang. In v4, results are never dropped. Instead, the oldest updates of subscriptions (e.g. telemetry) are dropped, so a slow subscriber gets recent data with gaps. The log message for this changed, see <a href="./troubleshooting.html#user_callbacks">Troubleshooting</a>.</p><p><strong>Callbacks can still run shortly after unsubscribing.</strong> This is not new, but easier to hit: a callback that is already queued still runs after <code>unsubscribe_...()</code> returns. Don&#39;t capture stack variables by reference in a callback that can outlive the function, e.g. a <code>std::promise</code> used to wait for one update. Capture a <code>std::shared_ptr</code> by value instead.</p><p><strong>Logging goes to stderr.</strong> MAVSDK&#39;s default log output is now written to stderr instead of stdout. The log callback API is unchanged, but many messages were reworded, so check any code that matches on message text.</p><p><strong>Timing and connections.</strong></p><ul><li>A system is discovered on its first heartbeat, about a second earlier than in v3, and MAVSDK sends its own first heartbeat as soon as a connection is added.</li><li>Sending over TCP and serial connections is asynchronous: a stalled peer no longer blocks MAVSDK, and write errors are reported through <code>subscribe_connection_errors()</code>. TCP client connections and hostname lookups happen in the background.</li><li><code>pass_received_raw_bytes()</code> processes the bytes asynchronously, and can be called from several threads.</li></ul><p><strong>MavlinkDirect JSON.</strong> The JSON is now always valid: strings are escaped, invalid UTF-8 is replaced, floats keep full precision, and <code>uint64</code> fields are no longer converted to signed. The <code>param_value</code> field of <code>PARAM_EXT_*</code> messages is now a byte array instead of a string.</p><p><strong>Gimbals</strong> are only announced once their <code>GIMBAL_DEVICE_INFORMATION</code> has been received (or a few seconds of retries have passed), so that vendor and model are filled in.</p><p><strong>Camera definitions</strong> from a vehicle are only looked up by file name, and the size of compressed definitions is limited.</p><h3 id="new-in-v4" tabindex="-1">New in v4 <a class="header-anchor" href="#new-in-v4" aria-label="Permalink to &quot;New in v4&quot;">​</a></h3><p>Not breaking, but worth knowing about:</p><ul><li>The <a href="./guide/heartbeat_watchdog.html">heartbeat watchdog</a>, to stop sending heartbeats when your application hangs.</li><li>tlog recording with <code>Mavsdk::start_tlog_recording()</code>.</li><li>The <code>MavlinkDirectServer</code> plugin, the server-side counterpart of MavlinkDirect.</li><li><code>Action::set_home()</code> and <code>Action::goto_location_fixedwing()</code>, camera focus control in <code>Camera</code> and <code>CameraServer</code>, mission upload with progress in <code>MissionRaw</code>.</li><li><code>to_string()</code> for all enums.</li><li>C bindings (<code>libcmavsdk</code>), a native Python binding, and Kotlin bindings, all in the MAVSDK repository.</li></ul><h2 id="v3" tabindex="-1">v3 <a class="header-anchor" href="#v3" aria-label="Permalink to &quot;v3&quot;">​</a></h2><h3 id="connections" tabindex="-1">Connections <a class="header-anchor" href="#connections" aria-label="Permalink to &quot;Connections&quot;">​</a></h3><p>In the past the connection syntax has caused quite a bit of confusion. Therefore, we decided to adapt the syntax for UDP and TCP connections slightly, mostly adopting the syntax that&#39;s already used in other software such as for instance pymavlink and mavlink-router.</p><p>The change makes it explicit whether we are listening on a port (think server), or connecting to a port (think client).</p><p>The new syntax for the 3 connection methods are described below:</p><h4 id="udp-connections" tabindex="-1">UDP connections <a class="header-anchor" href="#udp-connections" aria-label="Permalink to &quot;UDP connections&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>- UDP in  (server): udpin://our_ip:port</span></span>
<span class="line"><span>- UDP out (client): udpout://remote_ip:port</span></span></code></pre></div><p>For <code>udpin</code>, where we bind locally to that port, the IP can be set to:</p><ul><li><code>0.0.0.0</code> to listen on all network interfaces.</li><li><code>127.0.0.1</code> to listen on the local loopback interface only.</li><li>to our IP which means we listen on this network interfaces only.</li></ul><h4 id="tcp-connections" tabindex="-1">TCP connections <a class="header-anchor" href="#tcp-connections" aria-label="Permalink to &quot;TCP connections&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>- TCP in  (server):  tcpin://our_ip:port</span></span>
<span class="line"><span>- TCP out (client): tcpout://remote_ip:port</span></span></code></pre></div><p>For <code>tcpin</code>, where we listen locally on that port, the IP can be set to:</p><ul><li><code>0.0.0.0</code> to listen on all network interfaces.</li><li><code>127.0.0.1</code> to listen on the local loopback interface only.</li><li>to our IP which means we listen on this network interfaces only.</li></ul><h4 id="serial-connections" tabindex="-1">Serial connections <a class="header-anchor" href="#serial-connections" aria-label="Permalink to &quot;Serial connections&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>- Serial: serial://dev_node:baudrate</span></span>
<span class="line"><span>- Serial with flow control: serial_flowcontrol://dev_node:baudrate</span></span></code></pre></div><p>This is unchanged apart from the one caveat that the baudrate is no longer optional. This is to avoid cases where the baudrate is omitted by mistake.</p><h3 id="mavsdk-configuration" tabindex="-1">Mavsdk configuration <a class="header-anchor" href="#mavsdk-configuration" aria-label="Permalink to &quot;Mavsdk configuration&quot;">​</a></h3><p><code>ComponentType</code> is moved out of <code>Mavsdk</code> to its own scope, so the instantiation changes as follows:</p><p>Old instantiation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mavsdk mavsdk{Mavsdk::Configuration{Mavsdk::ComponentType::GroundStation}};</span></span></code></pre></div><p>New instantiation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mavsdk mavsdk{Mavsdk::Configuration{ComponentType::GroundStation}};</span></span></code></pre></div><h3 id="camera-plugin-major-redesign" tabindex="-1">Camera plugin: major redesign <a class="header-anchor" href="#camera-plugin-major-redesign" aria-label="Permalink to &quot;Camera plugin: major redesign&quot;">​</a></h3><p>The camera plugin API and internals have changed considerably. The aim was to be able to address multiple cameras from one plugin which enables more than one camera to be used in language wrappers such as Python. This wasn&#39;t previously possible.</p><p>Moreover, we now support MAVLink FTP and https to download the camera definition files, and not just http:</p><p>The API has changed to include a camera ID. Below is an example how usage changes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>auto camera = Camera{system};</span></span>
<span class="line"><span>camera.select_camera(0);</span></span>
<span class="line"><span></span></span>
<span class="line"><span>auto result = camera.zoom_range(component_id, 2.0f);</span></span></code></pre></div><p>To:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto camera = Camera{system};</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Assuming one camera is connected:</span></span>
<span class="line"><span>assert(camera.camera_list().cameras.size() == 1);</span></span>
<span class="line"><span>auto component_id = camera.camera_list().cameras[0].component_id;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>auto result = camera.zoom_range(component_id, 2.0f);</span></span></code></pre></div><h3 id="rtk-and-logstreaming-plugins-base64-for-binary-data" tabindex="-1">Rtk and LogStreaming plugins: base64 for binary data <a class="header-anchor" href="#rtk-and-logstreaming-plugins-base64-for-binary-data" aria-label="Permalink to &quot;Rtk and LogStreaming plugins: base64 for binary data&quot;">​</a></h3><p>With v2 it was hard or impossible to use APIs involving binary data in certain language wrappers, specifically Python. This meant that the RTK corrections and log streaming data were exposed as Python strings which didn&#39;t match the binary nature of the data.</p><p>Therefore, we are now using base64 for the data.</p><p>The assumption is that the performance hit of having to encode to and decode from base64 is negligible on platforms where MAVSDK is usually run.</p><p>Mavsdk provides helper functions to help with encoding and decoding base64:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#include &lt;mavsdk/base64.h&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>std::string encoded = ...;</span></span>
<span class="line"><span>std::vector&lt;uint8_t&gt; binary_data = base64_decode(encoded);</span></span></code></pre></div><h2 id="v2" tabindex="-1">v2 <a class="header-anchor" href="#v2" aria-label="Permalink to &quot;v2&quot;">​</a></h2><h3 id="mavsdk-configuration-1" tabindex="-1">Mavsdk configuration <a class="header-anchor" href="#mavsdk-configuration-1" aria-label="Permalink to &quot;Mavsdk configuration&quot;">​</a></h3><p>The <code>Mavsdk</code> class no longer assumes the configuration of a ground station by default but instead a configuration needs to be passed as a constructor argument.</p><p>The configuration argument sets the identification, so the MAVLink <code>MAV_TYPE</code> and MAVLink component ID (also called compid) of the Mavsdk instance itself, so the source and not the target that you&#39;re connecting to.</p><p>Old instantiation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mavsdk mavsdk;</span></span></code></pre></div><p>New instantiation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mavsdk mavsdk{Mavsdk::Configuration{Mavsdk::ComponentType::GroundStation}};</span></span></code></pre></div><div class="info custom-block"><p class="custom-block-title">INFO</p><p>It is still possible to change the configuration later (although not recommended) using <code>Mavsdk::set_configuration(...)</code>.</p></div><p><strong>Rationale:</strong></p><p>The reasons behind this change is to make users aware of this configuration and not assume any default. The problem with a ground station default is that it can be potentially dangerous without the user being aware. We are aware that this makes the API more cumbersome and verbose to use but the goal of Mavsdk is always to erro on the side of safety.</p><p>For instance, in the case where MAVSDK is used on the companion computer, the autopilot would assume that it is connected to a ground station at all time, even if the real ground station was lost.</p><p>Also, having to set the default makes it less likely to misconfigure the overall integration and end up with several components that conflict.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>It is also possible to set the sysid/compid directly, just check out the API of <a href="./cpp/api_reference/classmavsdk_1_1_mavsdk_1_1_configuration.html">Mavsdk::Configuration</a>.</p></div><h3 id="subscription-api" tabindex="-1">Subscription API <a class="header-anchor" href="#subscription-api" aria-label="Permalink to &quot;Subscription API&quot;">​</a></h3><p>The subscription API (also called async functions) has changed:</p><p>Old:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// Subscribe</span></span>
<span class="line"><span>telemetry.subscribe_position([](Telemetry::Position position) {</span></span>
<span class="line"><span>    std::cout &lt;&lt; &quot;Altitude: &quot; &lt;&lt; position.relative_altitude_m &lt;&lt; &quot; m\\n&quot;;</span></span>
<span class="line"><span>});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Unsubscribe again</span></span>
<span class="line"><span>telemetry.subscribe_position(nullptr);</span></span></code></pre></div><p>New:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// Subscribe</span></span>
<span class="line"><span>auto handle = telemetry.subscribe_position([](Telemetry::Position position) {</span></span>
<span class="line"><span>    std::cout &lt;&lt; &quot;Altitude: &quot; &lt;&lt; position.relative_altitude_m &lt;&lt; &quot; m\\n&quot;;</span></span>
<span class="line"><span>});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Unsubscribe again</span></span>
<span class="line"><span>telemetry.unsubscribe_position(handle);</span></span></code></pre></div><p><strong>Rationale:</strong> MAVSDK subscriptions had the drawback that there could only be one subscription per API. So subsequent subscribers would overwrite previous subscribers. With the new API there can be more than one subscriber per API, and each can unsubscribe using their own handle.</p><h3 id="server-component-plugins" tabindex="-1">Server component plugins <a class="header-anchor" href="#server-component-plugins" aria-label="Permalink to &quot;Server component plugins&quot;">​</a></h3><p>A lost of work has gone into Mavsdk v2 to support &quot;server&quot; components. These are components that typically run on the drone, e.g. a companion computer or a camera.</p><p>Server component plugins are all plugins that end in <code>_server</code>. They don&#39;t need a <code>System</code> to be instantiated as they are &quot;connected to and don&#39;t care who connects to them&quot;.</p><p>For instance a camera server plugin can be used as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mavsdk mavsdk{Mavsdk::Configuration{Mavsdk::ComponentType::Camera}};</span></span>
<span class="line"><span>auto camera_server = CameraServer{mavsdk.server_component()};</span></span></code></pre></div><p>It&#39;s also possible to add more than one MAVLink component to one MAVSDK instance, e.g. a gimbal could be added like this:</p><h3 id="connecting-to-first-autopilot-system" tabindex="-1">Connecting to first autopilot system <a class="header-anchor" href="#connecting-to-first-autopilot-system" aria-label="Permalink to &quot;Connecting to first autopilot system&quot;">​</a></h3><p>Discovering vehicles/systems required quite a bit of boilerplate code in the past. This has been simplified using a <code>Mavsdk::first_autopilot(double timeout_s)</code> method.</p><p>Old discovering of first autopilot:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>std::cout &lt;&lt; &quot;Waiting to discover system...\\n&quot;;</span></span>
<span class="line"><span>auto prom = std::promise&lt;std::shared_ptr&lt;System&gt;&gt;{};</span></span>
<span class="line"><span>auto fut = prom.get_future();</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// We wait for new systems to be discovered, once we find one that has an</span></span>
<span class="line"><span>// autopilot, we decide to use it.</span></span>
<span class="line"><span>mavsdk.subscribe_on_new_system([&amp;mavsdk, &amp;prom]() {</span></span>
<span class="line"><span>    auto system = mavsdk.systems().back();</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if (system-&gt;has_autopilot()) {</span></span>
<span class="line"><span>        std::cout &lt;&lt; &quot;Discovered autopilot\\n&quot;;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        // Unsubscribe again as we only want to find one system.</span></span>
<span class="line"><span>        mavsdk.subscribe_on_new_system(nullptr);</span></span>
<span class="line"><span>        prom.set_value(system);</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>});</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// We usually receive heartbeats at 1Hz, therefore we should find a</span></span>
<span class="line"><span>// system after around 3 seconds max, surely.</span></span>
<span class="line"><span>if (fut.wait_for(seconds(3)) == std::future_status::timeout) {</span></span>
<span class="line"><span>    std::cerr &lt;&lt; &quot;No autopilot found.\\n&quot;;</span></span>
<span class="line"><span>    return {};</span></span>
<span class="line"><span>}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>// Get discovered system now.</span></span>
<span class="line"><span>auto system = fut.get();</span></span></code></pre></div><p>New simplified way:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>auto system = mavsdk.first_autopilot(3.0);</span></span></code></pre></div><h2 id="v1" tabindex="-1">v1 <a class="header-anchor" href="#v1" aria-label="Permalink to &quot;v1&quot;">​</a></h2><h3 id="linking" tabindex="-1">Linking <a class="header-anchor" href="#linking" aria-label="Permalink to &quot;Linking&quot;">​</a></h3><p>With version 1, we introduce bigger changes in the way the MAVSDK library is linked to. Essentially, the library is no longer split up in multiple library files, one for the core (e.g. <code>mavsdk.so</code>) and one each for every plugin (e.g. <code>mavsdk_action.so</code>) and instead everything is merged into one library file (usually <code>mavsdk.so</code>).</p><p>This means that linking in cmake gets easier.</p><p>Previously, this was required:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target_link_libraries(my_executable</span></span>
<span class="line"><span>    MAVSDK::mavsdk_action</span></span>
<span class="line"><span>    MAVSDK::mavsdk_mission</span></span>
<span class="line"><span>    MAVSDK::mavsdk_telemetry</span></span>
<span class="line"><span>    MAVSDK::mavsdk</span></span>
<span class="line"><span>)</span></span></code></pre></div><p>Now, this is all that is needed:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>target_link_libraries(my_executable</span></span>
<span class="line"><span>    MAVSDK::mavsdk</span></span>
<span class="line"><span>)</span></span></code></pre></div><h4 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h4><p><strong>Why were the &quot;plugins&quot; split up in the first place?</strong></p><ul><li>The plugins were split because they were supposed to be extendable plugins. However, the last few years have shown that this is not really required, as there were no requests or contributions for it.</li><li>Also, having a real plugin architecture is not always easy. For instance, loading plugins at runtime with <code>dlopen</code> <a href="https://github.com/bpowers/musl/blob/master/src/ldso/dlopen.c" target="_blank" rel="noreferrer">is not possible</a> when the library is compiled with <a href="https://www.musl-libc.org/" target="_blank" rel="noreferrer">musl</a>. (We use musl to compile <code>mavsdk_server</code> statically without dependencies, so that it can be shipped with PyPi and runs on any Linux distribution and version as well as cross-compiled on armv6/7/8).</li></ul><p><strong>What are the advantages of the change?</strong></p><ul><li>Having one library to link to is easier and more inline with other libraries. When more functionality is needed, CMakeLists.txt doesn&#39;t need to be changed every time.</li><li>Having only one library makes it easier to check for ABI breaks.</li><li>The file overhead for every plugin is actually very small, so the overhead should be negligible.</li></ul>`,145)]))}const u=e(t,[["render",o]]);export{k as __pageData,u as default};
