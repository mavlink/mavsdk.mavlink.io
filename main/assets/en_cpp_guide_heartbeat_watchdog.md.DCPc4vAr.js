import{_ as a,o as t,c as s,ag as i}from"./chunks/framework.BpToWd9i.js";const g=JSON.parse('{"title":"Heartbeat Watchdog","description":"","frontmatter":{"github_branch":"main"},"headers":[],"relativePath":"en/cpp/guide/heartbeat_watchdog.md","filePath":"en/cpp/guide/heartbeat_watchdog.md"}'),n={name:"en/cpp/guide/heartbeat_watchdog.md"};function o(h,e,r,d,l,p){return t(),s("div",null,e[0]||(e[0]=[i(`<h1 id="heartbeat-watchdog" tabindex="-1">Heartbeat Watchdog <a class="header-anchor" href="#heartbeat-watchdog" aria-label="Permalink to &quot;Heartbeat Watchdog&quot;">​</a></h1><p>MAVSDK sends its own heartbeats whenever a system is connected or <a href="./../api_reference/classmavsdk_1_1_mavsdk_1_1_configuration.html">Configuration::set_always_send_heartbeats()</a> is set, and it keeps sending them for as long as the <code>Mavsdk</code> object exists — even if the application around it has hung or deadlocked. A peer therefore cannot tell a working application from a stuck one.</p><p>The heartbeat watchdog is a deadman timer that ties those heartbeats to application liveness instead: they are only sent while the application keeps calling <a href="./../api_reference/classmavsdk_1_1_mavsdk.html">Mavsdk::feed_heartbeat_watchdog()</a>. If the application stops feeding it, the heartbeats stop, and the peer sees the connection drop.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This is an opt-in feature, disabled by default. Most applications do not need it — reach for it when something on the other end of the link needs to act on the application being alive, e.g. an autopilot configured to fail-safe on GCS link loss.</p></div><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><p>The watchdog can be configured at startup, or enabled and changed later at runtime:</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mavsdk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Configuration</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ComponentType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GroundStation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_heartbeat_watchdog_timeout_s</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 0 disables it (the default)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Mavsdk</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mavsdk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mavsdk.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_heartbeat_watchdog_timeout_s</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // ... or at runtime</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// From wherever the application proves it is still healthy:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mavsdk.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">feed_heartbeat_watchdog</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>Both setters return <code>false</code> and keep the previous value if the timeout is invalid — it must be either 0 (disabled) or at least 2 seconds.</p><p>Heartbeats do not start until the watchdog has been fed at least once, and enabling the watchdog or changing its timeout stops them again until the next feed. The watchdog never grants a free timeout period. Once it has expired, heartbeats stay off until it is fed again, even if <code>set_always_send_heartbeats()</code> would otherwise allow them.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>A feed stays valid until the timeout elapses, including across system disconnect/reconnect and across periods where heartbeats are not being sent anyway. Feeding does not <em>start</em> heartbeats that are off for other reasons: with <code>always_send_heartbeats</code> unset and no system connected, nothing is sent regardless of the watchdog.</p></div><h2 id="how-often-to-feed" tabindex="-1">How often to feed <a class="header-anchor" href="#how-often-to-feed" aria-label="Permalink to &quot;How often to feed&quot;">​</a></h2><p>MAVSDK sends its periodic heartbeats at 1 Hz and checks the watchdog at each of those send opportunities, so a heartbeat goes out only if the last feed was less than the timeout ago.</p><p>Feed at least <em>twice</em> per timeout period rather than exactly once. If a feed lands slightly later than usual the next heartbeat is skipped, which leaves a gap of 2 s or more in the outgoing stream, and sustained jitter can make the peer declare the connection lost when the application is in fact fine.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>This is why the minimum timeout is 2 s rather than 1 s: at 1 s an application feeding once per second would have the feed and the heartbeat almost in phase, and a single late feed would already drop one. So feed at 1 Hz or faster, and leave the timeout at 2 s or above.</p></div><p>Feed from somewhere that genuinely stops if the application is unhealthy. Feeding from a dedicated timer thread that keeps running regardless defeats the purpose: it will keep the heartbeats alive while the rest of the application is deadlocked.</p><h2 id="use-from-language-wrappers" tabindex="-1">Use from language wrappers <a class="header-anchor" href="#use-from-language-wrappers" aria-label="Permalink to &quot;Use from language wrappers&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The rest of this page applies only to the language wrappers that drive MAVSDK through <em>mavsdk_server</em> over gRPC (e.g. Swift, Java, and the gRPC-based Python wrapper). Bindings that link <code>libmavsdk</code> directly do not go through mavsdk_server; they expose the watchdog through the C/C++ API above, and need their own binding for it.</p></div><p>For gRPC-based wrappers the watchdog lives on the mavsdk_server side, since that is the process holding the <code>Mavsdk</code> object and sending the heartbeats. It can be enabled either when starting the server:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>mavsdk_server --heartbeat-watchdog-timeout 2 udpin://0.0.0.0:14540</span></span></code></pre></div><p>or at runtime with the <code>SetHeartbeatWatchdogTimeout</code> RPC on the core service. Feeding is then done with the <code>FeedHeartbeatWatchdog</code> RPC.</p><p>Note what this arrangement does and does not cover: the watchdog tracks the liveness of whatever calls <code>FeedHeartbeatWatchdog</code>, so it detects a hung <em>application</em>, and it also covers the application losing its gRPC connection to mavsdk_server. It does not detect mavsdk_server itself hanging, because a stuck server stops sending heartbeats anyway.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The RPCs are only present in wrappers generated from a MAVSDK-Proto version that includes them. An older generated wrapper talking to a newer mavsdk_server will not have the methods.</p></div>`,22)]))}const k=a(n,[["render",o]]);export{g as __pageData,k as default};
